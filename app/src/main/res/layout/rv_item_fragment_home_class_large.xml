<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/itemView"
                android:layout_width="match_parent"
                android:layout_height="160dp"
                android:background="@drawable/ripple_bg">


    <TextView
        android:id="@+id/tv_f1_item_class_title"
        style="@style/WrapWrap"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="20dp"
        android:textColor="@color/black"
        android:textSize="18sp"
        tools:text="标题"/>


    <TextView
        android:id="@+id/tv_f1_item_class_sub_title"
        style="@style/WrapWrap"
        android:layout_below="@+id/tv_f1_item_class_title"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="6dp"
        tools:text="副标题"/>

    <ImageView
        android:id="@+id/iv_f1_item_class"
        android:layout_width="120dp"
        android:layout_height="match_parent"
        android:layout_below="@+id/tv_f1_item_class_sub_title"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="15dp"
        android:scaleType="fitXY"/>


</RelativeLayout>
